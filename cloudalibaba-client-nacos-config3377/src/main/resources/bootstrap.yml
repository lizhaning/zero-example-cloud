server:
  port: 3377

spring:
  application:
    name: client-nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: TEST_GROUP
        namespace: NS_FOR_DEV